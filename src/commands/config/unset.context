#!/usr/bin/env bash

# shellcheck disable=SC2034

DESCRIPTION="
Unsets a given property.

If this command is executed with an active profile, it unsets the property defined using that
profile. If multiple profiles are active, the last one provided on the command-line will be used.

"

OPTION_VALUE_TYPES=(
  %parameter-0 config-property
)
