#!/usr/bin/env bash

# shellcheck disable=SC2034

DESCRIPTION="
Updates the value of a given property.

If this command is executed with an active profile, the property is written using that profile. If
multiple profiles are active, the last one provided on the command-line will be used.
"

OPTION_VALUE_TYPES=(
  %parameter-0 config-property
  %parameter-1 value
)
