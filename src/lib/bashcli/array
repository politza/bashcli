#!/usr/bin/env bash

bashcli-array-sort() {
  local -n __array=$1
  shift
  local sortArgs=("$@")
  local IFS=$'\n'

  if [ ${#__array[@]} -gt 0 ]; then
    readarray -t __array <<<"$(LC_ALL=C sort "${sortArgs[@]}" <<<"${__array[*]}")"
  fi
}
