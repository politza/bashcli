#!/usr/bin/env bash
# shellcheck disable=SC2034


OPTIONS=(
  boolean     flag
  single      single
  multi-list  multi-list
  multi       multi
  enum        single
  primary     enum:enum
  secondary   enum:enum
)

OPTION_ALIASES=(
  b boolean
  s single
  m multi-list
  M multi
  e enum
)
