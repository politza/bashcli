#!/usr/bin/env bash
# shellcheck disable=SC2034

DESCRIPTION="\
Command description first line.
Command description second line.
Command description third line with trailing newlines.



"

OPTIONS=(
  boolean     boolean
  single      single
  multi-list  multi-list
  enum        single
  primary     enum:enum
)

OPTION_ALIASES=(
  a boolean
  c boolean
  b boolean
  s single
  m multi-list
  e enum
)

OPTION_DESCRIPTIONS=(
  boolean "Boolean option description."
  single  "Single option first line.
Single option second line.
Single option third line."
  multi-list "\
Multi option description."
  enum "Enum option description with trailing newlines.\


"
  primary "Primary option description."
)

OPTION_VALUE_TYPES=(
  boolean     boolean-value
  single      single-value
  multi-list  multi-list-value
  enum        enum-value
)
