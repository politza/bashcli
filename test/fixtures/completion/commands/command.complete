#!/usr/bin/env bash


bashcli-test-completions() {
  local prefix
  for prefix; do
    # shellcheck disable=SC2034
    BASHCLI_COMPLETIONS["${prefix}-completion"]="${prefix}-completion description"
  done
}

bashcli-complete-single() {
  COMPREPLY=(single-0 single-1)
}

bashcli-complete-multi-raw() {
  COMPREPLY=(multi-raw-0 multi-raw-1)
}

bashcli-complete-multi() {
  _comp_delimited , -W "multi-0 multi-1"
}

test-cli-complete-parameter() {
  COMPREPLY=(parameter-0 parameter-1)
}
